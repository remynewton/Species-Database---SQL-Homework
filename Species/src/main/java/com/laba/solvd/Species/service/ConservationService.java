package com.laba.solvd.Species.service;

import com.laba.solvd.Species.domain.ConservationStatus;

public interface ConservationService {
    ConservationStatus create (ConservationStatus conservationStatus);
}
